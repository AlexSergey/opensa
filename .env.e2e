# Committed e2e config. Loaded by `vite --mode e2e` (see playwright.config.ts); takes precedence over
# the local gitignored .env, so e2e always runs the deterministic FETCH loader.
VITE_STATIC_URL=http://localhost:3001
VITE_GAME_TYPE=original
VITE_ASSET_LOADER=fetch
